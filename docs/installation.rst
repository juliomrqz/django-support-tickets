============
Installation
============

At the command line::

    $ easy_install django-support-tickets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-support-tickets
    $ pip install django-support-tickets
